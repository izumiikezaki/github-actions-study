name: 毎日echo実行 #ここがワークフローのタイトル
on:
  schedule:
    - cron: "0 3 * * *" 
    # 毎日12:00に実行する
    # 時間はUTCであることに注意
jobs:
  Mainichi-Echo: #ここがジョブのタイトル
    runs-on: ubuntu-latest
    steps:
      - name: 🦀🦀🦀 #ここの内容がステップの見出しとなる。ない場合は「Run echo "これは定期実行のワークフローです。"」
        run: echo "毎日echoです。"
